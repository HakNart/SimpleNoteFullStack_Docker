# Full Stack Application: Simple note
### Run app using docker
**Important**
Set up the `.env` file as shown in the `.env.example`

```bash
docker compose up
```

The app is now running and can be accessed through `http://localhost:3000/`